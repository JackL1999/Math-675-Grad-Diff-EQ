\documentclass[12pt]{article}                         
\pagestyle{plain} %-- Document Set Up Command

%-- Document Setup & Layout
\usepackage{fancyhdr}
\usepackage{comment}
\usepackage{multicol}
\usepackage[
  letterpaper,
  left=0.8in,
  right=0.8in,
  textheight=9.5in,
  bmargin=0.75in  % Adjust this value to push the footer down
]{geometry}

%-- Color & Text Formatting
\usepackage[svgnames]{xcolor} 
\usepackage{textcomp}
\usepackage{ulem}
\usepackage{graphicx}

%-- Math Packages
\usepackage{amsmath}     
\usepackage{amsfonts}
\usepackage{amstext}
\usepackage{amssymb}
\usepackage{latexsym}
\usepackage{bm}

%-- Graphics and Figures
\usepackage{graphicx}    
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{circledtext}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usetikzlibrary{decorations.markings, arrows.meta}
%-- Tables
\usepackage{array}      
\usepackage{tabularx}

%-- Lists
\usepackage{enumitem}    
%\usepackage{enumerate}
\usepackage{tasks}

%-- TikZ Libraries
\usetikzlibrary{shapes.geometric} 
\pgfplotsset{compat=1.18}

\tikzset{
    phase arrow right/.style={
        postaction={decorate,
            decoration={
                markings,
                mark=at position 0.4 with {\arrow{Stealth}},
                mark=at position 0.9 with {\arrow{Stealth}}
            }
        }
    },
    phase arrow left/.style={
        postaction={decorate,
            decoration={
                markings,
                mark=at position 0.4 with {\arrow{Stealth[reversed]}},
                mark=at position 0.9 with {\arrow{Stealth[reversed]}}
            }
        }
    }
}
%-- Header/Footer
\pagestyle{fancy}
\fancyhf{} % Clear all header and footer fields
\fancyhead[L]{Jack Lingbeck} % Left header with name
\fancyhead[R]{February 10th, 2026} % Right header with date
\renewcommand{\headrulewidth}{0.4pt} % Horizontal line below the header
\rfoot{Page \thepage}
\lfoot{Math-675, Homework 3}

\begin{document}
\large
\begin{center}
\fbox{\begin{tabular}{c}
\bf Math 675 Spring 2026: \\
\bf Homework 3 (LATE) \\
\bf Due Tuesday, February 10th, 2026\\
\end{tabular}}
\end{center}

\section*{Question 1:}
\renewcommand{\arraystretch}{1.3}
\begin{enumerate}
\item[a)] $\mathbf{x'} = \begin{pmatrix} \frac{5}{4} & \frac{3}{4} \\ \frac{3}{4} & \frac{5}{4} \end{pmatrix} \mathbf{x}$
\subsection*{Finding the general Solution}    
    $\left| \begin{array}{cc} \frac{5}{4} - \lambda & \frac{3}{4}\\ \frac{3}{4} & \frac{5}{4} - \lambda\end{array}\right| 
    = \left(\frac{5}{4} - \lambda\right)^2 - \left(\frac{3}{4}\right)^2 = 0 \\\\
    \hspace*{95pt} = \left(\frac{5}{4} - \lambda + \frac{3}{4}\right)\left(\frac{5}{4} - \lambda - \frac{3}{4}\right) = 0 \\\\
    \hspace*{95pt} = \left(2 - \lambda\right)\left(\frac{1}{2} - \lambda\right) = 0 \Rightarrow \lambda_1 = 2, \lambda_2 = \frac{1}{2}$
    
    \subsubsection*{Eigenvalue \#1}
    $\mathbf{\lambda_1 = \phantom{-}2}: 
    \left[\begin{array}{cc} \frac{5}{4} - \lambda_1 & \frac{3}{4}\\ \frac{3}{4} & \frac{5}{4} - \lambda_1\end{array}\right]
    \rightarrow \left[\begin{array}{rr} -\frac{3}{4}& \frac{3}{4}\\ \frac{3}{4} & -\frac{3}{4} \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2cm}\begin{array}{rr} -1v_1 + 1v_2 &=0 \\ 1v_1 - 1v_2 &=0 \end{array} 
     \longrightarrow \quad v_1=v_2 \quad \longrightarrow \mathbf{v}_1 =\left[\begin{array}{cc} 1 \\ 1 \end{array}\right]$

    \subsubsection*{Eigenvalue \#2}
    $\mathbf{\lambda_2 = \phantom{-}\dfrac{1}{2}}: 
    \left[\begin{array}{cc} \frac{5}{4} - \lambda_2 & \frac{3}{4}\\ \frac{3}{4} & \frac{5}{4} - \lambda_2\end{array}\right]
    \rightarrow \left[\begin{array}{rr} \frac{3}{4}& \frac{3}{4}\\ \frac{3}{4} & \frac{3}{4} \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2cm}\begin{array}{rr} 1v_1 + 1v_2 &=0 \\ 1v_1 + 1v_2 &=0 \end{array} 
    \longrightarrow \quad v_1=-v_2 \quad \longrightarrow \mathbf{v}_2 =\left[\begin{array}{cc} -1 \\ 1 \end{array}\right]$

    So our general solution is $\mathbf{x} = c_1\begin{pmatrix} 1 \\ 1 \end{pmatrix}e^{2t} + c_2\begin{pmatrix} -1 \\ 1 \end{pmatrix}e^{\frac{1}{2}t}$
    \subsection*{Describing the behavior of the system}
    As $t \to \infty$, we can see that $e^{2t}$ approaches infinity significantly faster than $e^{0.5t}$
    ,so solutions will become parrellel to the line $y=x$ as $t \to \infty$.
    $$\lim_{t \to \infty} \frac{c_1e^{2t}v_1}{c_2e^{0.5t}v_2}  = \lim_{t \to \infty} \frac{c_1v_1}{c_2v_2}e^{1.5t} = \infty$$
    \subsection*{Sketching the phase portrait}
    We want to find the nullclines of the system, so we set each derivative to 0:
    \begin{flalign*}
        &\text{Set } x'=0:\text{so } 0 = \tfrac{5}{4}x+\tfrac{3}{4}y,\text{ giving us } y=-\tfrac{5}{3}x &\\
        &\text{Set } y'=0:\text{so } 0 = \tfrac{3}{4}x+\tfrac{5}{4}y, \text{ giving us } y=-\tfrac{3}{5}x &
    \end{flalign*}
    We still also have our initial eigenvalue directions of $y = x$ and $y=-x$
    \vspace{-1cm}
    \section*{Using technology to draw the phase portrait}
    \begin{figure}[H]
        \centering
        \includegraphics[width=0.4\textwidth]{Homework_3_Images/HW_3_Q1a.pdf}
    \end{figure}
    \begin{itemize}
        \item The black dashed line is the fast eigendirection $y=x$, which is the direction that trajectories will become parallel to as $t \to \infty$.
        \item The brown dashed line is the slow eigendirection $y=-x$. Trajectories emerge from the origin tangent to this line as $t \to -\infty$.
        \item The $x$-nullcline in red is $y=-\frac{5}{3}x$. Trajectories cross vertically.
        \item The $y$-nullcline in green is $y=-\frac{3}{5}x$. Trajectories cross horizontally.
        \item Trajectories between the nullclines spread outwards from the brown \\ eigendirection, and trajectories outside of the nullclines spread away to be parallel to the black eigendirection.
        
    \end{itemize}
    \vspace{12cm}


\item[b)] $\mathbf{x'} = \begin{pmatrix} 4 & -3\\ 8 & -6 \end{pmatrix} \mathbf{x}$
\subsection*{Finding the general Solution} 
    $\left| \begin{array}{cc} 4 - \lambda & -3\\ 8 & -6 - \lambda\end{array}\right| 
    = (4 - \lambda)(-6 - \lambda) - (-3)(8) = 0 \\\\
    \hspace*{108pt} = -24 - 4\lambda + 6\lambda + \lambda^2 + 24 = 0 \\\\
    \hspace*{108pt} = \lambda^2 + 2\lambda = 0 \\\\
    \hspace*{108pt} \Rightarrow \lambda_1 = 0, \lambda_2 = -2$

    \subsubsection*{Eigenvalue \#1}
    $\mathbf{\lambda_1 = \phantom{-}0}: 
    \left[\begin{array}{cc} 4 - \lambda_1 & -3\\ 8 & -6 - \lambda_1\end{array}\right]
    \rightarrow \left[\begin{array}{rr} 4 & -3\\ 8 & -6 \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.2cm}\begin{array}{rr} 4v_1 - 3v_2 &=0 \\ 8v_1 - 6v_2 &=0 \end{array} 
     \longrightarrow \quad 4v_1=3v_2 \quad \longrightarrow \mathbf{v}_1 =\left[\begin{array}{cc} 3 \\ 4 \end{array}\right]$

    \subsubsection*{Eigenvalue \#2}
    $\mathbf{\lambda_2 = -2}: 
    \left[\begin{array}{cc} 4 - \lambda_2 & -3\\ 8 & -6 - \lambda_2\end{array}\right]
    \rightarrow \left[\begin{array}{rr} 6 & -3\\ 8 & -4 \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.2cm}\begin{array}{rr} 6v_1 - 3v_2 &=0 \\ 8v_1 - 4v_2 &=0 \end{array} 
    \longrightarrow \quad 2v_1=v_2 \quad \longrightarrow \mathbf{v}_2 =\left[\begin{array}{cc} 1 \\ 2 \end{array}\right]$

    So our general solution is $\mathbf{x} = c_1\begin{pmatrix} 3 \\ 4 \end{pmatrix} + c_2\begin{pmatrix} 1 \\ 2 \end{pmatrix}e^{-2t}$
    \vspace{4cm}
    \subsection*{Describing the behavior of the system}
    As $t \to \infty$, we can see that ther single term $e^{-2t}$ approaches zero.
    But the first term does not change at all,so solutions will become parrellel to the line $y=\frac{1}{2}x$ as $t \to \infty$.
    $$\lim_{t \to \infty} \frac{c_2e^{-2t}v_2}{c_1v_1}  = 0$$
    \subsection*{Sketching the phase portrait}
    We want to find the nullclines of the system, so we set each derivative to 0:
    \begin{flalign*}
        &\text{Set } x'=0:\text{so } 0 = 4x-3y,\text{ giving us } y=\tfrac{4}{3}x &\\
        &\text{Set } y'=0:\text{so } 0 = 8x-6y, \text{ giving us } y=\tfrac{4}{3}x &
    \end{flalign*}
    We still also have our initial eigenvalue directions of $y = \frac{4}{3}x$ and $y=2x$
    \vspace{-1cm}
    \section*{Using technology to draw the phase portrait}
    \begin{figure}[H]
        \centering
        \includegraphics[width=0.4\textwidth]{Homework_3_Images/HW_3_Q1b.pdf}
    \end{figure}
    \begin{itemize}
        \item The brown dashed line is the eigendirection $y=2x$, which is the direction that trajectories will become parallel to as $t \to \infty$.
        \item The black dashed line is the eigendirection $y=\frac{4}{3}x$. \\ Trajectories are just unmoving singular points on this line.
        \item The green line are the $x-$ and $y$-nullclines $y=\frac{4}{3}x$. \\ Trajectories don't cross but stop at this line
        \item This means that every point along the green/black lines are equlibrium points, and trajectories that start off the line will approach the line
    \end{itemize}
    \vspace{12cm}


\item[c)] $\mathbf{x'} = \begin{pmatrix} 3 & -2 \\ 4 & -1 \end{pmatrix} \mathbf{x}$
    \subsection*{Finding the general Solution} 
    $\left| \begin{array}{cc} 3 - \lambda & -2\\ 4 & -1 - \lambda\end{array}\right| 
    = (3 - \lambda)(-1 - \lambda) - (-2)(4) = 0 \\\\
    \hspace*{95pt} = -3 - 3\lambda + \lambda + \lambda^2 + 8 = 0 \\\\
    \hspace*{95pt} = \lambda^2 - 2\lambda + 5 = 0 \\\\
    \hspace*{95pt} \Rightarrow \lambda_1 = 1+2i, \lambda_2 = 1-2i$

    \subsubsection*{Eigenvalue \#1}
    $\mathbf{\lambda_1 = 1+2i}: 
    \left[\begin{array}{cc} 3 - \lambda_1 & -2\\ 4 & -1 - \lambda_1\end{array}\right]
    \rightarrow \left[\begin{array}{cc} 2-2i& -2\\ 4 & -2-2i \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.2cm}\begin{array}{rr} (2-2i)v_1 - 2v_2 &=0 \\ 4v_1 - (2+2i)v_2 &=0 \end{array} 
     \longrightarrow \quad v_2=(1-i)v_1 \quad \longrightarrow \mathbf{v}_1 =\left[\begin{array}{cc} 1 \\ 1-i \end{array}\right]$

    So our general solution is $\mathbf{x} = c_1\begin{pmatrix} 1 \\ 1-i \end{pmatrix}e^{(1+2i)t}$ \\
    This just looks bad and its hard to interpret, so let's use Euler's formula to rewrite it in a better way: \\
    $\begin{pmatrix} 1 \\ 1-i \end{pmatrix}e^{(1+2i)t} = \begin{pmatrix} 1 \\ 1-i \end{pmatrix}e^{t}e^{2it}
    = \begin{pmatrix} 1 \\ 1-i \end{pmatrix}e^{t}(\cos(2t) + i\sin(2t)) \\\\
    = e^{t}\left[\begin{pmatrix} \cos(2t)+i\sin(2t) \\ (1-i)(\cos(2t)+i\sin(2t)) \end{pmatrix}\right] \\
    = e^{t}\left[\begin{pmatrix} \cos(2t) \\ \cos(2t) + \sin(2t) \end{pmatrix} + i\begin{pmatrix} \sin(2t) \\ \sin(2t) - \cos(2t) \end{pmatrix}\right] \\
    = c_1e^{t}\left[\begin{pmatrix} \cos(2t) \\ \cos(2t) + \sin(2t) \end{pmatrix}\right] + c_2e^{t}\left[\begin{pmatrix} \sin(2t) \\ \sin(2t) - \cos(2t) \end{pmatrix}\right] \\
    = c_1e^t \left[\begin{pmatrix} 1 \\ 1 \end{pmatrix} \cos 2t - \begin{pmatrix} 0 \\ -1 \end{pmatrix} \sin 2t \right] + c_2e^t \left[ \begin{pmatrix} 0 \\ -1 \end{pmatrix} \cos 2t + \begin{pmatrix} 1 \\ 1 \end{pmatrix} \sin 2t\right]$
    \\\\\\
    Notice that the vectors in front of the sins and cosines are components of the eigenvector that we started with:
    
    $$\mathbf{v}_1 =\left[\begin{array}{cc} 1 \\ 1-i \end{array}\right] =\left[\begin{array}{cc} 1 \\ 1 \end{array}\right] + i\left[\begin{array}{cc} 0 \\ -1 \end{array}\right]$$
    \\\\
    Since going through all of that work with complex eigenvalues, I wonder if we could build a potential shortcut?
    If $\lambda = \mathbf{a} + \mathbf{b}i$ is an eigenvalue of $A$ with eigenvector $\mathbf{v} = \mathbf{c} + i\mathbf{d}$, then we maybe can write the solution as 
    $$\mathbf{x}(t) = e^{\mathbf{a}t}(\mathbf{c}\cos(\mathbf{b}t) - \mathbf{d}\sin(\mathbf{b}t)) + e^{\mathbf{a}t}(\mathbf{d}\sin(\mathbf{b}t) + \mathbf{c}\cos(\mathbf{b}t))$$
    \subsection*{Describing the behavior of the system}
    As $t \to \infty$, we can see that both terms approach infinity.
    But due to the sins and cosines, and the nature of complex solutions, all trajectories will be spiraling outwards\\\\
    One thing I'm not quite sure of, maybe I missed it from the notes, but finding out the angle or tilt direction would be even more useful to describe the behavior of the system and to be able to better sketch it by hand.
    \subsection*{Sketching the phase portrait}
    We want to find the nullclines of the system, so we set each derivative to 0:
    \begin{flalign*}
        &\text{Set } x'=0:\text{so } 0 = 3x-2y,\text{ giving us } y=\tfrac{3}{2}x &\\
        &\text{Set } y'=0:\text{so } 0 = 4x-1y, \text{ giving us } y=4x &
    \end{flalign*}
    We still also have our initial eigenvalue directions but we can't plot them since they are complex
    \vspace{-1cm}
    \section*{Using technology to draw the phase portrait}
    \begin{figure}[H]
        \centering
        \includegraphics[width=0.4\textwidth]{Homework_3_Images/HW_3_Q1c.pdf}
    \end{figure}
    \begin{itemize}
        \item The eigenvalues are complex ($\lambda = 1 \pm 2i$). Since the real part is positive, the origin is an unstable spiral source.
        \item There are no real eigendirections for this system; instead, trajectories spiral outward from the origin.
        \item The red line is the $x$-nullcline $y = \frac{3}{2}x$. \\ Trajectories cross this line vertically.
        \item The green line is the $y$-nullcline $y = 4x$. \\ Trajectories cross this line horizontally.
    \end{itemize}
    \vspace{12cm}


\item[d)] $\mathbf{x'} = \begin{pmatrix} 2 & -\frac{5}{2} \\ \frac{9}{5} & -1 \end{pmatrix} \mathbf{x}$
    \subsection*{Finding the general Solution} 
    $\left| \begin{array}{cc} 2 - \lambda & -\frac{5}{2}\\ \frac{9}{5} & -1 - \lambda\end{array}\right| 
    = (2 - \lambda)(-1 - \lambda) - (-\frac{5}{2})(\frac{9}{5}) = 0 \\\\
    \hspace*{95pt} = -2 - 2\lambda + \lambda + \lambda^2 + \frac{9}{2} = 0 \\\\
    \hspace*{95pt} = \lambda^2 - \lambda + \frac{5}{2} = 0 \\\\
    \hspace*{95pt} \Rightarrow \lambda_1 = \frac{1}{2}+\frac{3}{2}i, \lambda_2 = \frac{1}{2}-\frac{3}{2}i$

    \subsubsection*{Eigenvalue \#1}
    $\mathbf{\lambda_1 = \dfrac{1}{2}+\dfrac{3}{2}i}: 
    \left[\begin{array}{cc} 2 - \lambda_1 & -\frac{5}{2}\\ \frac{9}{5} & -1 - \lambda_1\end{array}\right]
    \rightarrow \left[\begin{array}{cc} \frac{3}{2}-\frac{3}{2}i& -\frac{5}{2}\\ \frac{9}{5} & -\frac{3}{2}-\frac{3}{2}i \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.2cm}\begin{array}{rr} (\frac{3}{2}-\frac{3}{2}i)v_1 - \frac{5}{2}v_2 &=0 \\ \frac{9}{5}v_1 - (-\frac{3}{2}+\frac{3}{2}i)v_2 &=0 \end{array} 
     \rightarrow \quad (3-3i)v_1=5v_2 \quad \rightarrow \mathbf{v}_1 =\left[\begin{array}{cc} 5 \\ 3-3i \end{array}\right]$

    So our general solution is $\mathbf{x} = c_1\begin{pmatrix} 5 \\ 3-3i \end{pmatrix}e^{(\frac{1}{2}+\frac{3}{2}i)t}$ \\
    This just looks bad and its hard to interpret, so let's use Euler's formula to rewrite it in a better way: \\
    $\begin{pmatrix} 5 \\ 3-3i \end{pmatrix}e^{(\frac{1}{2}+\frac{3}{2}i)t} = \begin{pmatrix} 5 \\ 3-3i \end{pmatrix}e^{\frac{1}{2}t}e^{\frac{3}{2}it}
    = \begin{pmatrix} 5 \\ 3-3i \end{pmatrix}e^{t}(\cos(\frac{3}{2}t) + i\sin(\frac{3}{2}t)) \\\\
    = e^{t}\left[\begin{pmatrix} 5(\cos(\frac{3}{2}t)+i\sin(\frac{3}{2}t)) \\ (3-3i)(\cos(\frac{3}{2}t)+i\sin(\frac{3}{2}t)) \end{pmatrix}\right] \\
    = e^{t}\left[\begin{pmatrix} 5\cos(\frac{3}{2}t) \\ 3\cos(\frac{3}{2}t) + 3\sin(\frac{3}{2}t) \end{pmatrix} + i\begin{pmatrix} 5\sin(\frac{3}{2}t) \\ 3\sin(\frac{3}{2}t) - 3\cos(\frac{3}{2}t) \end{pmatrix}\right] \\
    = c_1e^{t}\left[\begin{pmatrix} 5\cos(\frac{3}{2}t) \\ 3\cos(\frac{3}{2}t) + 3\sin(\frac{3}{2}t) \end{pmatrix}\right] + c_2e^{t}\left[\begin{pmatrix} 5\sin(\frac{3}{2}t) \\ 3\sin(\frac{3}{2}t) - 3\cos(\frac{3}{2}t) \end{pmatrix}\right] \\
    = c_1e^t \left[\begin{pmatrix} 5 \\ 3 \end{pmatrix} \cos(\frac{3}{2}t) - \begin{pmatrix} 0 \\ -3 \end{pmatrix} \sin(\frac{3}{2}t) \right] + c_2e^t \left[ \begin{pmatrix} 0 \\ -3 \end{pmatrix} \cos(\frac{3}{2}t) + \begin{pmatrix} 5 \\ 3 \end{pmatrix} \sin(\frac{3}{2}t)\right]$
    \\\\\\
    Notice that the vectors in front of the sins and cosines are components of the eigenvector that we started with:
    
    $$\mathbf{v}_1 =\left[\begin{array}{cc} 5 \\ 3-3i \end{array}\right] =\left[\begin{array}{cc} 5 \\ 3 \end{array}\right] + i\left[\begin{array}{cc} 0 \\ -3 \end{array}\right]$$
    \\\\
    Notice that with another example, our hypothesized shortcut still works! This is pretty cool, so maybe we can just use this formula for any complex eigenvalues we get in the future and save ourselves a lot of time!
    \subsection*{Describing the behavior of the system}
    As $t \to \infty$, we can see that both terms approach infinity.
    But due to the sins and cosines, and the nature of complex solutions, all trajectories will be spiraling outwards\\\\
    One thing I'm not quite sure of, maybe I missed it from the notes, but finding out the angle or tilt direction would be even more useful to describe the behavior of the system and to be able to better sketch it by hand.
    \subsection*{Sketching the phase portrait}
    We want to find the nullclines of the system, so we set each derivative to 0:
    \begin{flalign*}
        &\text{Set } x'=0:\text{so } 0 = 2x-\tfrac{5}{2}y,\text{ giving us } y=\tfrac{4}{5}x &\\
        &\text{Set } y'=0:\text{so } 0 = \tfrac{9}{5}x-1y, \text{ giving us } y=\tfrac{9}{5}x &
    \end{flalign*}
    We still also have our initial eigenvalue directions but we can't plot them since they are complex
    \vspace{-1cm}
    \section*{Using technology to draw the phase portrait}
    \begin{figure}[H]
        \centering
        \includegraphics[width=0.4\textwidth]{Homework_3_Images/HW_3_Q1d.pdf}
    \end{figure}
    \begin{itemize}
        \item The eigenvalues are complex ($\lambda = \frac{1}{2} \pm \frac{3}{2}i$). Since the real part is positive, the origin is an unstable spiral source.
        \item There are no real eigendirections for this system; instead, trajectories spiral outward from the origin.
        \item The red line is the $x$-nullcline $y = \frac{4}{5}x$. \\ Trajectories cross this line vertically.
        \item The green line is the $y$-nullcline $y = \frac{9}{5}x$. \\ Trajectories cross this line horizontally.
    \end{itemize}
\end{enumerate}

\vspace{10cm}
\section*{Question 2:}
Find the general solution of the 3D System:
\renewcommand{\arraystretch}{1}
$$\mathbf{x'}(t) = \begin{pmatrix} 1 & 1 & 2 \\ 1 & 2 & 1 \\ 2 & 1 & 1 \end{pmatrix} \mathbf{x}(t)$$

$\left| \begin{array}{ccc} 1-\lambda & 1 & 2\\ 1 & 2-\lambda & 1 \\ 2 & 1 & 1-\lambda\end{array}\right|
= (1-\lambda)\left| \begin{array}{cc} 2-\lambda & 1\\ 1 & 1-\lambda\end{array}\right| - 1\left| \begin{array}{cc} 1 & 1 \\ 2 & 1-\lambda\end{array}\right| +2\left| \begin{array}{cc} 1 & 2-\lambda\\ 2 & 1\end{array}\right|$ \\
$\hspace*{115pt} = (1-\lambda)\left[(2-\lambda)(1-\lambda) - 1\right] - 1[(1-\lambda) - 2] + 2[1-2(2-\lambda)]=0$ \\\\
$\hspace*{115pt} = (1-\lambda)(1-3\lambda + \lambda^2) + (1+\lambda) + (-6+4\lambda)=0$ \\\\
$\hspace*{115pt} = 1 - 3\lambda + \lambda^2 - \lambda + 3\lambda^2 -\lambda^3 -5 + 5\lambda = 0$ \\\\
$\hspace*{115pt} = -\lambda^3 + 4\lambda^2 + \lambda - 4 = 0$ \\\\
$\hspace*{115pt} = \lambda^3 - 4\lambda^2 - \lambda + 4 = 0$ \\\\
$\hspace*{115pt} = (\lambda^3 - 4\lambda^2) - (\lambda - 4) = 0$ \\\\
$\hspace*{115pt} = \lambda^2(\lambda - 4) - (\lambda - 4) = 0$ \\\\
$\hspace*{115pt} = (\lambda^2 - 1)(\lambda - 4) = 0$ \\\\
$\hspace*{115pt} \text{So our eigenvalues are: } \lambda_1 = 4, \lambda_2 = 1, \lambda_3 = -1$\\

\vspace{10cm}
\subsubsection*{Eigenvalue \#1}
$\mathbf{\lambda_1 = \phantom{-}4}: 
      \left[ \begin{array}{ccc} 1 - \lambda_1 & 1 & 2\\1 & 2 - \lambda_1 & 1 \\ 2 & 1 & 1 - \lambda_1\end{array}\right]
    = \left[ \begin{array}{ccc} -3 & 1 & 2\\1 & -2 & 1 \\ 2 & 1 & -3\end{array}\right]
    \xrightarrow{\text{RREF}} \left[ \begin{array}{ccc} 1 & 0 & -1\\0 & 1 & -1 \\ 0 & 0 & 0\end{array}\right]$\\ 

$\hspace*{1.5cm}$ This gives us that $\left\{\begin{array}{rl} a &= c \\ b &= c \\ c &= c \end{array}\right.$
  so our eigenvalue is  $\mathbf{u}_1 = \left[ \begin{array}{c} 1\\ 1\\ 1\end{array}\right]$

\subsubsection*{Eigenvalue \#2}
$\mathbf{\lambda_2 = \phantom{-}1}: 
    \left[ \begin{array}{ccc} 1 - \lambda_2 & 1 & 2\\1 & 2 - \lambda_2 & 1 \\ 2 & 1 & 1 - \lambda_2\end{array}\right]
    = \left[ \begin{array}{ccc} 0 & 1 & 2\\1 & 1 & 1 \\ 2 & 1 & 0\end{array}\right]
    \xrightarrow{\text{RREF}} \left[ \begin{array}{ccc} 1 & 0 & -1\\0 & 1 & 2 \\ 0 & 0 & 0\end{array}\right]$\\ 

$\hspace*{1.5cm}$ This gives us that $\left\{\begin{array}{rl} a &= c \\ b &= -2c \\ c &= c \end{array}\right.$
  so our eigenvalue is  $\mathbf{u}_2 = \left[ \begin{array}{c} 1\\ -2\\ 1\end{array}\right]$

\subsubsection*{Eigenvalue \#3}
$\mathbf{\lambda_3 = -1}: 
    \left[ \begin{array}{ccc} 1 - \lambda_3 & 1 & 2\\1 & 2 - \lambda_3 & 1 \\ 2 & 1 & 1 - \lambda_3\end{array}\right]
    = \left[ \begin{array}{ccc} 2 & 1 & 2\\1 & 3 & 1 \\ 2 & 1 & 2\end{array}\right]
    \xrightarrow{\text{RREF}} \left[ \begin{array}{ccc} 1 & 0 & 1\\0 & 1 & 0 \\ 0 & 0 & 0\end{array}\right]$\\ 

$\hspace*{1.5cm}$ This gives us that $\left\{\begin{array}{rl} a &= -c \\ b &= 0 \\ c &= c \end{array}\right.$
  so our eigenvalue is  $\mathbf{u}_3 = \left[ \begin{array}{c} -1\\ 0\\ 1\end{array}\right]$\\

\noindent
Thus, the general solution to our system is 
$$\mathbf{x}(t) = c_1e^{4t}\left[ \begin{array}{c} 1\\ 1\\ 1\end{array}\right] + c_2e^{t}\left[ \begin{array}{c} 1\\ -2\\ 1\end{array}\right] + c_3e^{-t}\left[ \begin{array}{c} -1\\ 0\\ 1\end{array}\right]$$

\vspace{10cm}

\section*{Question 3:}
Consider the equation $ay'' + by' + cy = 0$ with $a, b, c$ are constants and $a \neq 0$. \\
The general solution depends on the roots of the characteristic equation $ar^2+br+c$
\begin{enumerate}
    \item[a)] Given $x_1 = y, x_2 = y'$, we can then also say that $x_1' = y' = x_2$ and $x_2' = y''$ \\
            so we can rewrite our original equation as $ax_2' + bx_2 + cx_1 = 0$ \\
            Solving for the derivative we obtain $x_2' = -\dfrac{b}{a}x_2 - \dfrac{c}{a}x_1$ with $x_1'=x_2$ \\
            Thus, we can write our system as $\mathbf{x'} = \begin{pmatrix} 0 & 1 \\ -\frac{c}{a} & -\frac{b}{a} \end{pmatrix}\mathbf{x}$ \\

    \item[b)] Now let's find the eigenvalues of this system: \\
            $\left| \begin{array}{cc} -\lambda & 1 \\ -\frac{c}{a} & -\frac{b}{a} - \lambda\end{array}\right| 
            = (-\lambda)(-\dfrac{b}{a} - \lambda) + \dfrac{c}{a} = 0 \\\\
            \hspace*{95pt} = \lambda^2 + \dfrac{b}{a}\lambda + \dfrac{c}{a} = a\lambda^2 +b\lambda + c = 0$\\
            Notice that this is the same characteristic equation as our original second order ODE, so the eigenvalues are just the roots of the characteristic equation. \\
\end{enumerate}

\vspace{20cm
}
\section*{Question 4:}
Consider the system:
$$\mathbf{x'} = \begin{pmatrix} -1 & -1 \\ -\alpha & -1 \end{pmatrix} \mathbf{x}$$
\begin{enumerate}
    \item[a)] Solve the system for $\alpha = 0.5$ What are the eigenvalues? Classify the equilibrium at the origin. \\
    $\left| \begin{array}{cc} -1 - \lambda & -1\\ -0.5 & -1 - \lambda\end{array}\right| 
    = \left(-1 - \lambda\right)^2 - (-1)(-0.5)= 0 \\\\
    \hspace*{95pt} = 1 + 2\lambda + \lambda^2 -0.5 = 0 \\\\
    \hspace*{95pt} = \lambda^2 + 2\lambda + 0.5 = 0 \\\\
    \hspace*{95pt} \Rightarrow \lambda_1 = -1 + \frac{\sqrt{2}}{2}, \lambda_2 = -1 - \frac{\sqrt{2}}{2}$

    \subsubsection*{Eigenvalue \#1}
    $\mathbf{\lambda_1 = -1 + \frac{\sqrt{2}}{2}}: 
    \left[\begin{array}{cc} -1 - \lambda_1 & -1\\ -0.5 & -1 - \lambda_1\end{array}\right]
    \rightarrow \left[\begin{array}{rr} -\frac{\sqrt{2}}{2}& -1\\ -0.5 & -\frac{\sqrt{2}}{2} \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.8cm}\begin{array}{rr} -\frac{1}{\sqrt{2}}v_1 - 1v_2 &=0 \\ -0.5v_1 - \frac{1}{\sqrt{2}}v_2 &=0 \end{array} 
     \longrightarrow \quad v_1=-\sqrt{2}v_2 \quad \longrightarrow \mathbf{v}_1 =\left[\begin{array}{cc} -\sqrt{2} \\ 1 \end{array}\right]$

    \subsubsection*{Eigenvalue \#2}
    $\mathbf{\lambda_2 = -1 - \frac{\sqrt{2}}{2}}: 
    \left[\begin{array}{cc} -1 - \lambda_2 & -1\\ -0.5 & -1 - \lambda_2\end{array}\right]
    \rightarrow \left[\begin{array}{rr} \frac{\sqrt{2}}{2} & -1\\ -0.5 & \frac{\sqrt{2}}{2} \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.8cm}\begin{array}{rr} \frac{1}{\sqrt{2}}v_1 - 1v_2 &=0 \\ -0.5v_1 + \frac{1}{\sqrt{2}}v_2 &=0 \end{array} 
     \longrightarrow \quad v_1=\sqrt{2}v_2 \quad \longrightarrow \mathbf{v}_1 =\left[\begin{array}{cc} \sqrt{2} \\ 1 \end{array}\right]$

    So our general solution is $\mathbf{x} = c_1\begin{pmatrix} -\sqrt{2} \\ 1 \end{pmatrix}e^{(-1 + \frac{\sqrt{2}}{2})t} + c_2\begin{pmatrix} \sqrt{2} \\ 1 \end{pmatrix}e^{(-1 - \frac{\sqrt{2}}{2})t}$ \\
    The equlibrium at the origin is a stable sink node, since both eigenvalues are negative and real. \\
    
    \vspace{8cm}
    \item[b)] Repeat for $\alpha = 2$ \\
    $\left| \begin{array}{cc} -1 - \lambda & -1\\ -2 & -1 - \lambda\end{array}\right| 
    = \left(-1 - \lambda\right)^2 - (-1)(-2)= 0 \\\\
    \hspace*{95pt} = 1 + 2\lambda + \lambda^2 -2 = 0 \\\\
    \hspace*{95pt} = \lambda^2 + 2\lambda -1 = 0 \\\\
    \hspace*{95pt} \Rightarrow \lambda_1 = -1 + \sqrt{2}, \lambda_2 = -1 - \sqrt{2}$

    \subsubsection*{Eigenvalue \#1}
    $\mathbf{\lambda_1 = -1 + \sqrt{2}}: 
    \left[\begin{array}{cc} -1 - \lambda_1 & -1\\ -2 & -1 - \lambda_1\end{array}\right]
    \rightarrow \left[\begin{array}{rr} -\sqrt{2}& -1\\ -2 & -\sqrt{2} \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.8cm}\begin{array}{rr} -\sqrt{2}v_1 - 1v_2 &=0 \\ -2v_1 - \sqrt{2}v_2 &=0 \end{array} 
     \longrightarrow \quad v_2=-\sqrt{2}v_1 \quad \longrightarrow \mathbf{v}_1 =\left[\begin{array}{cc} 1 \\ -\sqrt{2} \end{array}\right]$

    \subsubsection*{Eigenvalue \#2}
    $\mathbf{\lambda_2 = -1 - \sqrt{2}}: 
    \left[\begin{array}{cc} -1 - \lambda_2 & -1\\ -2 & -1 - \lambda_2\end{array}\right]
    \rightarrow \left[\begin{array}{rr} \sqrt{2} & -1\\ -2 & \sqrt{2} \end{array}\right] \left[\begin{array}{cc} v_1 \\ v_2 \end{array}\right] = \left[\begin{array}{cc} 0 \\ 0 \end{array}\right]$

    $\hspace*{2.8cm}\begin{array}{rr} \sqrt{2}v_1 - 1v_2 &=0 \\ -2v_1 + \sqrt{2}v_2 &=0 \end{array} 
     \longrightarrow \quad v_2=\sqrt{2}v_1 \quad \longrightarrow \mathbf{v}_1 =\left[\begin{array}{cc} 1 \\ \sqrt{2} \end{array}\right]$

    So our general solution is $\mathbf{x} = c_1\begin{pmatrix} 1 \\ -\sqrt{2} \end{pmatrix}e^{(-1 + \sqrt{2})t} + c_2\begin{pmatrix} 1 \\ \sqrt{2} \end{pmatrix}e^{(-1 - \sqrt{2})t}$ \\
    The equlibrium at the origin is a unstable saddle, since we have one positive and one negative real eigenvalue. \\
    
    \vspace{12cm}

    \item[c)] Find the eigenvalues in terms of $\alpha$ and determine the value of $\alpha$ between $0.5$ and $2$ where the transition from one type of behavior to the other occurs. \\
    \item[] $\left| \begin{array}{cc} -1 - \lambda & -1\\ -\alpha & -1 - \lambda\end{array}\right| 
    = \left(-1 - \lambda\right)^2 - \alpha= 0 \\\\
    \hspace*{95pt} \rightarrow \left(-1 - \lambda\right)^2 = \alpha \\\\
    \hspace*{95pt} \rightarrow -1 - \lambda = \pm \sqrt{\alpha} \\\\
    \hspace*{95pt} \rightarrow -1 - \lambda = \pm \sqrt{\alpha} \\\\
    \hspace*{95pt} \rightarrow \lambda_1 = -1 + \sqrt{\alpha}, \lambda_2 = -1 - \sqrt{\alpha}$ \\\\
    We can see that $\lambda_1 > \lambda_2$ for all $\alpha > 0$. Notice that for all values of $\alpha$, $\lambda_2$ is always negitive, but the same cant be said for $\lambda_1$. 
    So the transition from stable to unstable behavior occurs when $\lambda_1$ goes from negative to positive, which happens at $\alpha = 1$. 
    Any values of $\alpha$ between $0$ and $1$ will give us both negitive eigenvalues, meaning the origin is a stable sink node,
    and any values of $\alpha$ between $1$ and $2$ will give us an unstable saddle since we will obtain one positive and one negative eigenvalue. \\
\end{enumerate}

\end{document}